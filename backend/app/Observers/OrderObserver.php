<?php

namespace App\Observers;

use App\Jobs\SyncServer;
use App\Order;
use App\Services\Notifiers\ApiNotifier;
use App\Services\Notifiers\CreateOrderNotifiers;
use App\Services\Notifiers\WebApiNotifier;
use Carbon\Carbon;

class OrderObserver
{
    /**
     * Handle the order "created" event.
     *
     * @param  \App\Order  $order
     * @return void
     */
    public function created(Order $order)
    {
        $order->OrderCode = Carbon::parse($order->OrderDate)
            ->format('Y-m').'-'.$order->OrderId;
        
        $order->save();

        (new CreateOrderNotifiers($order))->handle();
    }

    /**
     * Handle the order "updated" event.
     *
     * @param  \App\Order  $order
     * @return void
     */
    public function updated(Order $order)
    {
        //
    }

    /**
     * Handle the order "deleted" event.
     *
     * @param  \App\Order  $order
     * @return void
     */
    public function deleted(Order $order)
    {
        //
    }

    /**
     * Handle the order "restored" event.
     *
     * @param  \App\Order  $order
     * @return void
     */
    public function restored(Order $order)
    {
        //
    }

    /**
     * Handle the order "force deleted" event.
     *
     * @param  \App\Order  $order
     * @return void
     */
    public function forceDeleted(Order $order)
    {
        //
    }
}
